const ProjectModel = require('../models/ProjectModel.js');

module.exports = {
    getOverviewForUser(req, res) {
        return async function () {
            try {
                console.log('Fetching overview');
            } catch (err) {
                
            }
        }();
    }
};
